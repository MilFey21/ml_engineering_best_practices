# Gradient Boosting model configuration
name: gradient_boosting
type: GradientBoostingClassifier

# Hyperparameters
params:
  n_estimators: 100
  max_depth: 5
  learning_rate: 0.1
  subsample: 0.8
  min_samples_split: 5
  min_samples_leaf: 2
  max_features: "sqrt"
  random_state: ${seed}
